---
import Layout from "../../layouts/Layout.astro";
import FormattedDate from "../../components/FormattedDate.astro";
import ProyectoCard from "../../components/ProyectoCard.astro";
import { getCollection, render } from "astro:content";
import type { GetStaticPaths } from "astro";

export const getStaticPaths = (async () => {
  const proyectos = await getCollection("proyectos");
  return proyectos.map((proyecto) => ({
    params: { slug: proyecto.id },
    props: { proyecto },
  }));
}) satisfies GetStaticPaths;

const { proyecto } = Astro.props;
const { Content } = await render(proyecto);
const { title, cliente, descripcion, problema, resultado, stack, fecha, duracion, role, repo, url, tags, metricas } =
  proyecto.data;

const allProyectos = await getCollection("proyectos");
const related = allProyectos
  .filter((p) => p.id !== proyecto.id && p.data.tags.some((t) => tags.includes(t)))
  .sort((a, b) => b.data.fecha.getTime() - a.data.fecha.getTime())
  .slice(0, 3);
---

<Layout
  title={`${title} — Proyecto de ${cliente}`}
  description={descripcion}
  pageType="article"
>
  <article>
    <header class="pt-12 md:pt-20 pb-10 md:pb-14 border-b border-[var(--color-border)]">
      <div class="container-page max-w-4xl reveal">
        <a
          href="/proyectos"
          class="tactile inline-flex items-center gap-1.5 text-sm text-[var(--color-mute)] hover:text-[var(--color-ink)] mb-8 font-mono"
        >
          <span aria-hidden="true">←</span> Volver a proyectos
        </a>

        <div class="flex items-center flex-wrap gap-3 mb-5 text-xs font-mono text-[var(--color-mute)]">
          <span class="text-[var(--color-dorado)] font-semibold">{cliente}</span>
          <span class="w-1 h-1 rounded-full bg-[var(--color-border)]"></span>
          <FormattedDate date={fecha} format="long" />
          <span class="w-1 h-1 rounded-full bg-[var(--color-border)]"></span>
          <span>{duracion}</span>
        </div>

        <h1 class="font-display font-bold tracking-tight mb-6 text-[var(--color-ink)]">
          {title}
        </h1>
        <p class="text-xl text-[var(--color-ink-soft)] leading-relaxed max-w-3xl">
          {descripcion}
        </p>

        <div class="mt-8 flex flex-wrap gap-2">
          {tags.map((t) => (
            <span class="font-mono text-xs px-2.5 py-1 rounded border border-[var(--color-border)] text-[var(--color-ink-soft)]">
              #{t}
            </span>
          ))}
        </div>
      </div>
    </header>

    {metricas.length > 0 && (
      <section class="py-10 md:py-12 bg-[var(--color-ink)] text-[var(--color-blanco-humo)]">
        <div class="container-page reveal">
          <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-blanco-humo)]/60 mb-6">
            Resultados medibles
          </p>
          <dl class="grid grid-cols-2 md:grid-cols-4 gap-6">
            {metricas.map((m) => (
              <div class="border-l border-[var(--color-dorado)] pl-4">
                <dd class="font-impact text-3xl md:text-4xl text-[var(--color-blanco)] uppercase tabular-nums tracking-tight leading-none">
                  {m.value}
                </dd>
                <dt class="font-mono text-[10px] uppercase tracking-widest text-[var(--color-blanco-humo)]/60 mt-2 leading-tight block">
                  {m.label}
                </dt>
              </div>
            ))}
          </dl>
        </div>
      </section>
    )}

    <div class="container-page py-12 md:py-16">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 lg:gap-16">
        <aside class="lg:col-span-1 space-y-7 text-sm reveal">
          <div>
            <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-mute)] mb-2">
              Cliente
            </p>
            <p class="text-[var(--color-ink)]">{cliente}</p>
          </div>
          <div>
            <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-mute)] mb-2">
              Rol
            </p>
            <p class="text-[var(--color-ink)]">{role}</p>
          </div>
          <div>
            <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-mute)] mb-2">
              Duración
            </p>
            <p class="text-[var(--color-ink)]">{duracion}</p>
          </div>
          <div>
            <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-mute)] mb-2">
              Stack
            </p>
            <ul class="flex flex-wrap gap-1.5">
              {stack.map((s) => (
                <li class="font-mono text-[11px] px-2 py-0.5 rounded bg-[var(--color-blanco-humo)] border border-[var(--color-border)] text-[var(--color-ink-soft)]">
                  {s}
                </li>
              ))}
            </ul>
          </div>
          {(url || repo) && (
            <div class="pt-2 border-t border-[var(--color-border)]">
              <p class="font-mono text-[11px] uppercase tracking-widest text-[var(--color-mute)] mb-3">
                Links
              </p>
              <div class="flex flex-col gap-2">
                {url && (
                  <a
                    href={url}
                    rel="noopener noreferrer"
                    target="_blank"
                    class="tactile text-[var(--color-dorado)] hover:text-[var(--color-ink)] inline-flex items-center gap-1"
                  >
                    Ver sitio en vivo <span aria-hidden="true">↗</span>
                  </a>
                )}
                {repo && (
                  <a
                    href={repo}
                    rel="noopener noreferrer"
                    target="_blank"
                    class="tactile text-[var(--color-dorado)] hover:text-[var(--color-ink)] inline-flex items-center gap-1"
                  >
                    Repositorio en GitHub <span aria-hidden="true">↗</span>
                  </a>
                )}
              </div>
            </div>
          )}
        </aside>

        <div class="lg:col-span-2 space-y-12 reveal">
          <section>
            <p class="font-mono text-xs uppercase tracking-widest text-[var(--color-mute)] mb-3">
              01 · El problema
            </p>
            <h2 class="font-display font-bold text-2xl md:text-3xl tracking-tight text-[var(--color-ink)] mb-4">
              {problema}
            </h2>
          </section>

          <section>
            <p class="font-mono text-xs uppercase tracking-widest text-[var(--color-mute)] mb-3">
              02 · Lo que entregué
            </p>
            <div class="prose-editorial">
              <Content />
            </div>
          </section>

          <section>
            <p class="font-mono text-xs uppercase tracking-widest text-[var(--color-mute)] mb-3">
              03 · El resultado
            </p>
            <h2 class="font-display font-bold text-2xl md:text-3xl tracking-tight text-[var(--color-ink)] mb-4">
              {resultado}
            </h2>
          </section>

          <div class="pt-2 border-t border-[var(--color-border)]">
            <a
              href="/contacto"
              class="tactile inline-flex items-center justify-center gap-2 px-6 py-3.5 rounded-md bg-[var(--color-ink)] text-[var(--color-azul)] font-semibold hover:bg-[var(--color-dorado)] hover:text-[var(--color-blanco)] border-2 border-[var(--color-ink)] hover:border-[var(--color-dorado)]"
            >
              Hablemos de un proyecto similar
              <span aria-hidden="true">→</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </article>

  {related.length > 0 && (
    <section class="py-16 md:py-24 border-t border-[var(--color-border)] bg-[var(--color-blanco)]">
      <div class="container-page">
        <h2 class="font-display font-bold tracking-tight mb-10 text-[var(--color-ink)] reveal">
          Proyectos similares.
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-5 reveal-stagger">
          {related.map((p) => <ProyectoCard proyecto={p} />)}
        </div>
      </div>
    </section>
  )}
</Layout>
